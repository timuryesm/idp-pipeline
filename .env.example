# Copy to .env and adjust as needed. NEVER commit your real .env.
IDP_MAX_FILE_SIZE_MB=10
IDP_MIN_FILE_SIZE_BYTES=100
IDP_LOG_LEVEL=INFO
# IDP_UPLOAD_DIR=/abs/path/to/uploads
# IDP_QUARANTINE_DIR=/abs/path/to/quarantine
# IDP_LOG_DIR=/abs/path/to/logs

# Reserved for Step 2 (AI extraction) — leave blank for now:
OPENAI_API_KEY=
GEMINI_API_KEY=
